// Implement a function named power that takes a base and an exponent as 
// arguments and returns the result of raising  the base to the power of the exponent.

let power=(a,b)=>{
    let base=a;
    let exponent=b;
    return a**b;
}
let result=power(4,3);
console.log(result);
